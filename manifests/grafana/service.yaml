apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: ecoplan
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
  # Cluster-only — access via: kubectl port-forward svc/grafana 3000:3000 -n ecoplan
